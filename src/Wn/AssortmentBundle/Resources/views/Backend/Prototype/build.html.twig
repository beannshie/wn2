{% extends 'SandboxCoreBundle:Backend:layout.html.twig' %}

{% block header %}
    <h1>Tworzenie produktu z prototypu {% filter lower %}{{ prototype.name }}{% endfilter %}</h1>
{% endblock %}

{% block content %}
<form action="{{ path('sylius_assortment_backend_prototype_build', {'id': prototype.id}) }}" method="post" class="form-horizontal" {{ form_enctype(form) }} novalidate>
    {% include 'SandboxAssortmentBundle:Backend/Product:_form.html.twig' %}
    {{ form_widget(form._token) }}
    <div class="form-actions">
       <input type="submit" value="utwórz {% filter lower %}{{ prototype.name }}{% endfilter %}" class="btn btn-primary btn-large" />
    </div>
</form>
{% endblock %}
