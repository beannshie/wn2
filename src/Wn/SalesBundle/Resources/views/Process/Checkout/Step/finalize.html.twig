{% extends 'SandboxCoreBundle:Frontend:layout.html.twig' %}

{% block content %}
<div class="page-header">
<h1>Podsumowanie <small>Sfinalizuj zamówienie</small></h1>
</div>

{% include 'SandboxSalesBundle:Process/Checkout/Step:_progressBar.html.twig' %}

<div class="form-horizontal">
    <div class="row-fluid">
        <div class="span6">
            <table class="table table-bordered">
                <thead>
                    <tr>
                        <th colspan="2">Adres dostawy</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td><strong>imię</strong></td>
                        <td>{{ order.deliveryAddress.firstname }}</td>
                    </tr>
                    <tr>
                        <td><strong>nazwisko</strong></td>
                        <td>{{ order.deliveryAddress.lastname }}</td>
                    </tr>
                    <tr>
                        <td><strong>ulica</strong></td>
                        <td>{{ order.deliveryAddress.street }}</td>
                    </tr>
                    <tr>
                        <td><strong>miasto</strong></td>
                        <td>{{ order.deliveryAddress.city }}</td>
                    </tr>
                    <tr>
                        <td><strong>kod pocztowy</strong></td>
                        <td>{{ order.deliveryAddress.postcode }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
        <div class="span6">
            <table class="table table-bordered">
                <thead>
                    <tr>
                        <th colspan="2">Adres na rachunku</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td><strong>imię</strong></td>
                        <td>{{ order.billingAddress.firstname }}</td>
                    </tr>
                    <tr>
                        <td><strong>nazwisko</strong></td>
                        <td>{{ order.billingAddress.lastname }}</td>
                    </tr>
                    <tr>
                        <td><strong>ulica</strong></td>
                        <td>{{ order.billingAddress.street }}</td>
                    </tr>
                    <tr>
                        <td><strong>miasto</strong></td>
                        <td>{{ order.billingAddress.city }}</td>
                    </tr>
                    <tr>
                        <td><strong>kod pocztowy</strong></td>
                        <td>{{ order.billingAddress.postcode }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
    <table class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>#</th>
                <th>produkt</th>
                <th>ilość</th>
                <th><span class="pull-right">cena jednostkowa</span></th>
                <th><span class="pull-right">suma</span></th>
            </tr>
        </thead>
        <tbody>
            {% for item in order.items %}
            <tr>
                <td>{{ loop.index }}</td>
                <td>
                    <p>{{ item.variant.product.name }}</p>
                    {% if item.variant.countOptions > 0 %}
                    <ul class="unstyled">
                        {% for option in item.variant.options %}
                        <li><strong>{{ option.presentation }}</strong>: {{ option.value  }}</li>
                        {% endfor %}
                    </ul>
                    {% endif %}
                </td>
                <td>{{ item.quantity }}</td>
                <td>
                    <span class="pull-right">
                    {{ item.unitPrice }} zł
                    </span>
                </td>
                <td>
                    <span class="pull-right">
                    {{ item.unitPrice * item.quantity }} zł
                    </span>
                </td>
            </tr>
            {% endfor %}
            <tr>
                <td colspan="4">
                </td>
                <td colspan="2">
                    <span class="pull-right">
                    <strong>Suma</strong>: {{ order.total }} zł
                    </span>
                </td>
            </tr>
        </tbody>
    </table>
    <div class="form-actions">
        <a href="{{ path('sylius_sandbox_checkout_display', {'stepName': context.previousStep.name}) }}" class="btn btn-large">powrót</a> &nbsp;
        <a href="{{ path('sylius_sandbox_checkout_forward', {'stepName': context.currentStep.name}) }}" class="btn btn-large btn-success">zamów</a> &nbsp;
    </div>
</div>
{% endblock %}
