{% extends 'FreeNoteBundle:Backend:layout.html.twig' %}

{% block header %}
    <h1>Taxonomy updating <small>Use this form to edit existing taxonomy</small></h1>
{% endblock %}

{% block content %}
<form action="{{ path('free_note_backend_taxonomy_update', {'id': taxonomy.id}) }}" method="post" class="form-horizontal" novalidate>
    {% include 'FreeNoteBundle:Backend/Taxonomy:_form.html.twig' %}
    <div class="form-actions">
       <input type="submit" value="save changes" class="btn btn-large btn-primary" />
       &nbsp;
       <a href="{{ path('free_note_backend_taxonomy_list')}}" class="btn btn-large">back to list</a>
    </div>
</form>
{% endblock %}
