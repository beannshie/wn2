{% extends 'FreeNoteBundle:Backend:layout.html.twig' %}

{% block header %}
    <h1>Post details</h1>
{% endblock %}

{% block content %}
<div class="well">
<a href="{{ path('free_note_backend_article_entry_update', {'id': post.id}) }}" class="btn">
    <i class="icon-pencil"></i> update
</a>
<a href="{{ path('free_note_backend_article_entry_delete', {'id': post.id}) }}" class="btn btn-danger confirmer"
    data-toggle="modal"
    data-target="confirmer-modal"
    data-confirmer-question="Are you sure you want to delete this post?"
>
    <i class="icon-remove icon-white"></i> delete
</a>
</div>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>#id</th>
            <th>title</th>
            <th>author</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>{{ post.id }}</td>
            <td>{{ post.title }}</td>
            <td>{{ post.author }}</td>
        </tr>
    </tbody>
</table>

<div class="well">
<h2>Post content.</h2>
{{ post.content|markdown }}
</div>

{% endblock %}
